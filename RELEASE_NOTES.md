# StarsHub v1.1.1

## 修复
- 侧边栏缩窄（200px -> 152px），视图名过长显示省略号
- 视图右键菜单：编辑配置/重命名/删除（带确认）
- 编辑配置弹框支持重新选择维度选项
- 新建 API 配置默认输入框为空（不再预填当前激活配置）
- 自定义 AI 端点自动补全 /chat/completions 路径（修复 404）
- 自定义 AI 配置卡片改为可展开折叠编辑
- httpPost 支持 SSL 自签名证书 + gzip 响应解压
- 嵌入配置连接状态持久显示
- 插件启动时自动验证 Token/AI/Embedding 连接
- 提交信息去掉版本号前缀，版本号只在 Release 标题
